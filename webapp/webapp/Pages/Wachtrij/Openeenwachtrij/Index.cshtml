@page
@model webapp.Pages.Wachtrij.Openeenwachtrij.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Wachtrij[0].Peercoach)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Wachtrij[0].Lokaal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Wachtrij[0].Tijdvak)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Wachtrij) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Peercoach)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lokaal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tijdvak)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
